interface:
  display_name: "Project Handoff 2.1"
  short_description: "维护有证据的项目交接，支持原始记录前置筛选与灰区复核，供新会话续接"
  default_prompt: "Use $project-handoff to verify the active project facts and create, preview, or update 项目开发交接.md in the current project directory."
policy:
  allow_implicit_invocation: false
